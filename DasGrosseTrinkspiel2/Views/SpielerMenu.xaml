<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DasGrosseTrinkspiel2.Views.SpielerMenu">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Spieler" 
                TextColor="White"
                FontSize="50"
                HorizontalOptions="Center"
                Padding="0,20,0,0"
                />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="7"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Entry Grid.Column="0" Grid.Row="0"/>
                <Picker Title="Geschlecht" ItemsSource="{Binding Genders}" SelectedItem="Männlich" 
                        Grid.Column="1"
                        Grid.Row="0"/>
                <Button Text="Hinzufügen" Grid.Row="0" Grid.Column="2" />
                <CollectionView Grid.ColumnSpan="3" Grid.Row="1" ItemsSource="{Binding Gamers}">
                    
                </CollectionView>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>